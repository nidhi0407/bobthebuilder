# ForeSite AI — Person 1 (CV & Tracking) Requirements
# Core lightweight perception dependencies (optimized for instant install & zero conflicts)
opencv-python-headless>=4.8.0
numpy>=1.24.0
onnxruntime>=1.15.0
pytest>=7.0.0

# Optional: Full Ultralytics PyTorch backend (when disk space and PyTorch environment are configured)
# ultralytics>=8.0.0
# torch>=2.0.0
# torchvision>=0.15.0
